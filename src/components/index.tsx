import React from "react";
import { Page } from "./layout/page";

export const IndexPage: React.FunctionComponent = React.memo(() => {
	return (
		<Page title="Radiator Alternator">
			<h2>Title ID</h2>
		</Page>
	);
});
